export const selectPublications = (state) => state.publications.items;
export const selectIsLoading = (state) => state.publications.isLoading;
export const selectError = (state) => state.publications.error;
